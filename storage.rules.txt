rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allows any authenticated user to read and write to storage.
      // This is insecure for production but good for development.
      allow read, write: if request.auth != null;
    }
  }
}